---
title: "Amit Kushwaha - Benchmarking the Agent Era: Measuring Performance Beyond the LLM - AI Native DevCon"
channel: "AI Native Dev"
video_id: guhTp2Q8VX0
url: https://www.youtube.com/watch?v=guhTp2Q8VX0
published: 2026-07-10T07:24:37+00:00
generated: 2026-07-13T06:25:35+00:00
model: "z-ai/glm-5.2"
thumbnail: https://i.ytimg.com/vi/guhTp2Q8VX0/hqdefault.jpg
---
# Amit Kushwaha - Benchmarking the Agent Era: Measuring Performance Beyond the LLM - AI Native DevCon

[![Amit Kushwaha - Benchmarking the Agent Era: Measuring Performance Beyond the LLM - AI Native DevCon](https://i.ytimg.com/vi/guhTp2Q8VX0/hqdefault.jpg)](https://www.youtube.com/watch?v=guhTp2Q8VX0)

[Watch on YouTube](https://www.youtube.com/watch?v=guhTp2Q8VX0) · **AI Native Dev** · 2026-07-10

## TL;DR
Amit Kushwaha, a solutions architect at NVIDIA, explains that traditional LLM benchmarks—designed for single-turn chatbots with fixed input/output shapes—fail to capture the complexity of modern agentic workloads. He outlines key optimizations like prefix caching, KV-cache-aware routing, and speculative decoding, and argues that meaningful agent benchmarking requires multi-turn trajectories, tool-call awareness, distribution-based SLOs, and steady-state measurement. He highlights Artificial Analysis's upcoming agent benchmark as an early attempt to close this gap, while noting that multi-agent orchestration, quality-under-load, and heterogeneous workloads remain unsolved challenges.

## Key Takeaways
- **Chatbot vs. Agent Era:** Old chatbot workloads were single-turn with small context (1k–4k tokens) and no tools; agentic workloads involve dozens of turns, context lengths in the tens to hundreds of thousands, heavy tool usage, and a mix of GPU (LLM) and CPU (tool) execution.
- **Coding Agent Example:** A single user request (e.g., "fix a flaky test") can trigger 20–40 turns of LLM–tool interaction, where most tokens are input (tool outputs fed back) and output tokens are small (just tool-call instructions).
- **Prefix Caching is Essential:** By the later turns of a trajectory, most of the input context is repeated from prior turns and can be cached; only the newly generated tokens require GPU prefill work. Without caching, time-to-first-token (TTFT) degrades severely.
- **KV-Cache-Aware Routing:** In multi-replica deployments, requests from the same session must land on the same replica to reuse the stored KV cache; round-robin routing defeats this optimization.
- **Speculative Decoding Accelerates Decode:** A small draft model predicts multiple tokens that the large target model verifies in a single pass, yielding roughly 3x speedup; modern models increasingly ship with built-in multi-token prediction (MTP) draft heads.
- **Current Benchmarks Miss the Mark:** Existing benchmarks use fixed input/output shapes, no tool calls, single-turn requests, and disable key optimizations—none of which reflect real agentic behavior.
- **Distributions, Not Means:** Agentic performance must be evaluated via histograms and percentiles (P95, P25), not averages; tail latency directly impacts user experience.
- **Percentile Choice Matters:** Speculative decoding shifts the token-speed distribution rightward (faster) but can look worse on P5; choosing the wrong percentile can unfairly penalize a beneficial optimization.
- **Tool Calls Affect Concurrency:** Ignoring CPU-side tool execution overestimates GPU utilization; in reality, GPUs idle during tool calls, meaning hardware can support more concurrent users than tool-free benchmarks suggest.
- **Steady-State Measurement:** Agentic metrics (TTFT, token speed) start in a transient state while caches warm up; benchmarks must wait for metrics to stabilize before declaring pass/fail against SLOs.
- **Artificial Analysis Agent Benchmark:** A new third-party benchmark (covering DeepSeek V4 and GPT-OSS 120B) enables multi-turn trajectories, caching, speculative decoding, and SLO-based concurrency measurement, with per-watt and per-dollar metrics forthcoming.
- **Open Challenges:** Multi-agent workflows, quality-under-load (accuracy coupled with speed), long-running sessions with growing cache memory, heterogeneous workloads, CPU-GPU coupling, and task-completion-time metrics remain unresolved.

## Detailed Breakdown

**[00:00] Introduction**
The host introduces Amit Kushwaha, a solutions architect at NVIDIA, who will speak on benchmarking in the agent era. Kushwaha frames the talk around NVIDIA's focus on inference performance and the need for benchmarks that represent real-world workloads.

**[01:02] From Chatbots to Agents: What Changed**
Kushwaha contrasts the old chatbot era—single-turn, 1k–4k context, small outputs, no tools—with the agentic era: dozens of turns, context lengths exploding to hundreds of thousands, output tokens in the thousands, and heavy tool usage. He argues that benchmarks scoring yesterday's chatbot workloads say nothing about today's real agentic workloads.

**[03:07] Anatomy of an Agentic Workload**
Using a coding agent example ("fix a flaky test"), Kushwaha walks through a trajectory where the LLM issues tool calls (grep, bash, file reads), feeds results back, and iterates for 20–40 turns. GPU runs the LLM (green); CPU runs tools (purple). Most tokens are input from tool outputs; LLM output tokens are small because they are just tool-call instructions.

**[05:44] Optimization 1: Prefix Caching**
Because later turns repeat most of the prior context, prefix caching lets the system skip recomputation of already-processed input. By turn 40, the vast majority of input tokens are cached; only newly generated tokens require GPU prefill work. Without caching, TTFT and overall performance suffer significantly.

**[07:18] Optimization 2: KV-Cache-Aware Routing**
When a model is served on multiple replicas, round-robin routing may send turn 2 to a different replica than turn 1, losing the cached KV state. KV-cache-aware routing (via session ID or cache-aware scheduling) ensures subsequent turns land on the same replica, preserving the cache. An audience question clarifies that KV refers to the key/value matrices used to store LLM conversation history.

**[09:24] Optimization 3: Speculative Decoding**
On the decode (output) side, a small draft model predicts multiple tokens (e.g., four) that the large target model verifies in a single pass, producing roughly three tokens per step instead of one—a ~3x speedup. Kushwaha notes that modern models increasingly bundle draft heads (multi-token prediction, MTP) so the optimization works out of the box.

**[12:00] What Current Benchmarks Miss**
Existing benchmarks use fixed input/output shapes, no tool calls, single-turn requests, and disable caching, routing, and speculative decoding. This fails to represent agentic reality: trajectories grow dynamically, multi-turn is absent, optimizations are off, and tools are ignored—so measured performance does not reflect real workloads.

**[13:35] Performance as Distributions, Not Averages**
Kushwaha presents TTFT and token-speed histograms. TTFT: left (fast) is good, right (slow) is bad. Token speed: left (slow) is bad, right (fast) is good. Tail latency (P95) drives user experience; means and medians are insufficient. He stresses the need to think in percentiles and distributions.

**[15:44] Percentile Choice and Speculative Decoding**
Comparing token-speed distributions with speculative decoding on (green) vs. off (blue), the green distribution is generally faster. However, at P5, green looks worse—an artifact of the optimization shifting the distribution. Choosing the wrong percentile can unfairly penalize a beneficial optimization; Kushwaha's team found P25 more appropriate, underscoring the need to deeply analyze how optimizations affect distributions relative to SLO targets.

**[18:17] Tool Calls and Concurrency**
Ignoring tool calls overestimates GPU utilization. In reality, GPUs idle while tools run on CPU. If roughly half the timeline is CPU tool execution, the system can support roughly twice the concurrent users a tool-free benchmark would suggest. Accurate workload representation requires modeling these CPU-side gaps.

**[20:55] Steady-State vs. Transient Metrics**
Agentic metrics evolve while caches warm up. Measuring during the transient state can falsely indicate SLO failure (e.g., TTFT exceeding 10 seconds) and trigger unnecessary GPU additions. Kushwaha advises plotting metrics over time and waiting for flatness (steady state) before evaluating pass/fail.

**[22:32] Artificial Analysis Agent Benchmark**
Kushwaha highlights a new third-party benchmark from Artificial Analysis that enables KV caching, multi-turn trajectories, speculative decoding, and SLO-based concurrency measurement. Initially covering DeepSeek V4 and GPT-OSS 120B, it asks: for a given SLO (TTFT, token speed), how many users can a given hardware configuration sustain? Per-watt and per-dollar metrics are forthcoming.

**[24:34] What's Still Missing**
Kushwaha catalogs open challenges: multi-agent orchestration, quality-under-load (coupling accuracy with speed), long-running sessions with continuously growing cache memory, heterogeneous (non-coding) workloads, CPU-GPU coupling dynamics, and task-completion-time metrics. He concludes that benchmarks have caught up to the chatbot world but agentic benchmarking is just beginning.

**[26:43] Q&A**
An audience member asks whether speculative decoding works with non-zero temperature. Kushwaha confirms it does: the draft model still samples from a distribution that matches the target model, so higher probability of correct prediction holds even with temperature. A second question clarifies that the histograms are drawn from hundreds of trajectories, not a single prompt, and that prefix caching constrains TTFT to only the new uncached tokens even for hard problems with long tool outputs.

## Notable Quotes
- "The benchmark that scored yesterday on this site doesn't say anything about the real workloads that currently exist."
- "Most of the tokens that are generated are actually at this step. When you are calling the tool, this is where most of the tokens are being generated."
- "Caching becomes kind of a must-have in most of these agentic workflows."
- "You are actually penalizing a very good optimization where most of your requests are actually sitting on the right side just because you chose the wrong [percentile]."
- "If you ignore the tools part of it, you're not really measuring the right workload."
- "Today's benchmarks have caught up to the chatbot world, but the benchmarks for the agentic world are still being built, and we are just getting started."

## People, Tools & References Mentioned
- **Amit Kushwaha** — Solutions Architect, NVIDIA (speaker)
- **NVIDIA** — Inference optimization focus
- **Artificial Analysis** — Third-party benchmarking company; upcoming agent benchmark
- **DeepSeek V4** — Model included in the new agent benchmark
- **GPT-OSS 120B** — Model included in the new agent benchmark
- **KV cache** — Key/value matrices storing LLM conversation history
- **Prefix caching** — Optimization for reusing previously processed input tokens
- **KV-cache-aware routing / session-based routing** — Ensuring turns land on the replica holding the cache
- **Speculative decoding / Multi-token prediction (MTP)** — Draft model accelerates target model decode
- **SLOs (Service Level Objectives)** — TTFT and token-speed targets used for benchmark evaluation
- **P5, P25, P95** — Percentile metrics for distribution-based performance analysis

## Who Should Watch
ML infrastructure engineers, inference serving teams, and benchmarking researchers who need to evaluate hardware or software stacks against realistic agentic workloads rather than legacy chatbot benchmarks. The talk is especially relevant to anyone deploying coding agents or tool-using LLM systems where caching, routing, and decode optimizations determine real-world capacity and user experience.


<details class="transcript">
<summary>Full transcript</summary>

<p>I would like you to give it up for Attit Kushwar from Nvidia. Now whilst Ait is getting set up, I will just put it on top.</p>
<p>Realize I haven&#x27;t got all my notes on my machine. So you&#x27;re going to be talking about benchmarking the benchmarking the agent era and you&#x27;re ready to go. So I will leave</p>
<p>and you&#x27;re ready to go. So I will leave you to it.</p>
<p>Thank you very much.</p>
<p>Thank you very much. Ait.</p>
<p>My name is Amit Kushwa. I&#x27;m a solutions architect at Nvidia. Um at NVIDIA we live and breathe inference optimize inference performance. Uh and one of the most important thing that we care about to have benchmarks that represent the real world workload. However, these benchmarks have been changing over last six last one and a half year. We started with simple chatbot applications where now we are purely into the agent era where it&#x27;s no more single input output.</p>
<p>where it&#x27;s no more single input output. It&#x27;s much more complex. Workflows are running, agents are calling multiple tools dealing with multiple context lens. However, the benchmarking has not kept up with it. So, in the next 30 minutes, I&#x27;m going to talk about three three things. One, what does the agentic benchmark look agentic workload looks like? Second, what are the optimizations that already exist to run these agentic workloads efficiently? And third, what do the current benchmarks actually miss? And I&#x27;m going to conclude with giving an</p>
<p>And I&#x27;m going to conclude with giving an example of a giving an example of a benchmark that is trying to close that gap and then also share some learnings on what what is in the future, what is still missing and what we still need to kind of cover up. Uh before we start, let&#x27;s look at what exactly has changed. So on the left, what you see is the old kind of old chatbot era. When I say old chatbot era, what you mean is most of the conversations were single turn. You ask a question, you get a response back. Context lengths were very small from 1k</p>
<p>Context lengths were very small from 1k to 4k tokens. The output tokens were also small. Depending on what kind of questions you&#x27;re asking, you typically get a shorter response. So the context lens that the LMS were producing as output was much smaller. Whereas those there was no concept of tools. These agents or these applications were not using tools. Now in the agentic workloads what we are seeing is dozens of turns and I&#x27;ll give you concrete examples of what what I mean. U the LLMs are getting called</p>
<p>mean. U the LLMs are getting called multiple times. The sequence lengths have exploded from tens of thousands to hundreds of thousands sequence length. The output tokens are also increased from hundreds to thousands of tokens that these LLMs are producing. And we are using tools heavily. Most of these agentic workloads are using different kinds of tool to achieve the task that we have asked them to do. So the benchmark that scored yesterday on this site doesn&#x27;t say anything about the real workloads that can currently exist.</p>
<p>real workloads that can currently exist. And before we talk about benchmarking agentic workflows, let look let&#x27;s look at a simple example of what an agentic workload looks like. Here the the user is actually asking the LLM to fix a flaky test in in their codebase. So this is a user input. After that you see a series of calls. This is where the LLM takes the user input comes back with a response and the response in this case is oh go and check this</p>
<p>this case is oh go and check this certain file and grab grabs certain things in this file. Based on that response now a tool is run and this tool could be anything that is sitting on your Linux terminal. It could be bash scripts. It could be um grabs. It could be web search and then it keeps on doing that. It feeds that input back to the LLM. LM looks at the new information, provides a new response and tell its to go and look into the next file, then the tool goes and lets into the next file. So this this whole trajectory where you have a single user</p>
<p>trajectory where you have a single user input in the beginning can go on for 20 to 40 turns depending on how complex the workload is. And a few things to note here. The portion in green is what is being run by an LLM or or is running on a GPU. The portion on per in purple is typically run on on CPUs. So it&#x27;s kind of a mix of a workload where GPU and CPU kind of interplay to solve your whole problem. And just to reiterate what I said, you say one user message, dozens of tool calls.</p>
<p>of tool calls. Most of the tokens that are generated are actually at this step. When you are calling the tool, this is where most of the tokens are being generated. The tool is calling multiple files, reading multiple files, and then feeding that information back into the LLM. So that&#x27;s where kind of the most tokens are input and the output tokens are very small and the reason being the output of the LLMs are typically just calling tools. They&#x27;re just just giving small instructions on which tool to which tool to call and how to call. So typically what you see is the output tokens are</p>
<p>what you see is the output tokens are much smaller than what we would have seen in the older uh era. And again this is an example for a coding agent. So the talk will mostly focus on the coding agent side of things. This workload might diff look different if you&#x27;re talking about different kinds of agent but here the focus is primarily on the coding agent side. So looking at that workload there are few optimizations that are already kind of uh present in the production uh systems and these are very critical to make that workload work. Again going</p>
<p>make that workload work. Again going back to kind of the turns setup that I shared in the previous slide. uh in the first turn this is your user input and you get an output in the second turn this whole thing could be cached and this is the optimization that I&#x27;m that I&#x27;m talking about for most of the agentic workloads caching becomes very very important because what you are doing is by the by the time you are at the 40th turn a lot of context has been</p>
<p>the 40th turn a lot of context has been created but you don&#x27;t need to do all the work if you are caching your outputs inputs properly then you only need to do work on the new tokens that are being generated in this whole workflow. So at the end of kind of this this fourth turn for example most of this is cached and you don&#x27;t have to do any work on it. The new part is only where you run your GPUs and actually do make the work. If the caching was not enabled you can imagine a lot more work needs to be done and there&#x27;s</p>
<p>needs to be done and there&#x27;s consequences on on performance on time to first token and different kinds of metrics. So caching becomes kind of a must-have in most of these agentic workflows. And as as you see the vast majority of output token input tokens by the end could be cached because it&#x27;s just building up on the previous history. The caching itself is not the full story. Especially when you are serving these models on multiple replicas, your turns have to kind of get on the</p>
<p>your turns have to kind of get on the right replica. For example, if your turn one in the previous slide landed on replica one and this is just an example of you could have a model served multiple times. So these are three replicas of the same model that you&#x27;re using in your agentic workloads. So turn one end up on replica A. If you do a roundroin kind of a setup, the turn two might end up on some other replica, but this replica does not have the cache that we talked about. So the other thing that becomes very important in multi-replica uh kind of setup is to</p>
<p>multi-replica uh kind of setup is to make sure that your turns land on the same replica where your cache is stored. So this is what is kind of showed here in the KVAware routing where turn one lands on replica one. We make sure that the turn two also lands on replica one so that we can leverage the KV cache that is stored already on that side. And so there are multiple ways of doing that. There&#x27;s session ID based where you just keep track of the session and make sure whatever request was served on one</p>
<p>sure whatever request was served on one replica goes to the same replica or you can do more KB cache aware kind of setup. Yeah,</p>
<p>I have a stupid question. KB what is that?</p>
<p>Ah KB. So KV is just K andV matrices you can think about in the LLM world. This is how you store the history and it&#x27;s kind of a maybe I should have explained it. It&#x27;s a very generic term that is used in LM world is you can think of it just storing the history of what you have already done. So the both these optimizations that I talked about are on the prefill side. So</p>
<p>talked about are on the prefill side. So this is where the input is getting to your LLMs. Now the other the third kind of optimization that that goes into a gentic workload is on the decode side. So there are two workloads for a large language model. One is when you provide your input and the LLM is just digesting the input and the second is when the LLM starts producing the output. So the first set of prefix caching and KV cache aware primarily deals on the pre on the prefill side or the input side. This optimization is on the output side. When</p>
<p>optimization is on the output side. When the LLM starts producing the output, how can you make that output go as fast as possible? And this is where this optimization on speculative decoding is becoming very very common. The high level idea is this is the input to your big model and what happens is a small model called the draft model helps this big model to go fast and the way it does it you provide the input the draft model which is much smaller can run way faster than</p>
<p>is much smaller can run way faster than the larger model. So it produces these four tokens. It says brown fox hopped over. The job of target model, the big model is just to verify these tokens and it can do that in in a single request and that&#x27;s where the efficiency comes in. So you start with an input, the small model goes and predicts four tokens in advance. The big model comes in and just checks all these four tokens in one go. And this is where you get the efficiency. So in the end what ends up</p>
<p>efficiency. So in the end what ends up happening is you produce three tokens in just one pass. Whereas if you didn&#x27;t have this step of uh speculative decoding the large model would have had to produce these tokens one by one. So roughly you can think you get three times the speed up just by using speculative decoding. And there are multiple flavors of speculative decoding. One is where actually you go and train these draft models. you have you use the data from the big model and then train these small draft models.</p>
<p>then train these small draft models. What is happening more and more these draft models are coming packed with the model itself. So you might have heard about the terminological multi-token prediction MTPs which is basically these draft tokens coming packaged with new models. So you can essentially just turn it on and your model should be able to use this optimization right away without you having to do much. So as I said three tokens in one pass and and I I&#x27;ll come to the caveat and I</p>
<p>and and I I&#x27;ll come to the caveat and I have I have a slide that will show what what happens when you do this. But essentially I I talked about three uh optimizations prefix caching cache aware and then speculative decoding. The reason I talked about them because these are kind of the foundational optimization that you do for agentic workloads. Whereas if you look at the current existing benchmarks, it doesn&#x27;t capture all the complexity that I just talked about. Most of the current benchmarks used</p>
<p>Most of the current benchmarks used fixed shapes. That means if you have done any sort of benchmarking, if you&#x27;re trying to answer the question, oh, I have a particular workload, how many GPUs I need? What typically is done? You choose your ISL and OSL and it&#x27;s typically a fixed value. So you you keep the shape fixed. You don&#x27;t have any tool calls. This this is on the left is basically the current status quo of the what the benchmarking looks like. Fixed input output no tool calls and most of the optimizations that I just talked about are turned off</p>
<p>I just talked about are turned off and single turn. So no concept of multi-turn where you&#x27;re talking about trajectory which is completely different from what we see in agentic workloads on the agentic workloads. Now what we miss there&#x27;s no fixed shape as I saw the trajectory keeps growing as you are interacting with your system. It&#x27;s collecting information from the tool and the information to the LM is continuously growing. So the fixed shape it misses the fixed shape. There&#x27;s no concept of multi-turn in this original benchmarking world. Most of the optimizations that I talked about prefix</p>
<p>optimizations that I talked about prefix caching KV aware uh and and speculative decoding are mostly turned off and no tools and that&#x27;s where the bo that&#x27;s where the main issue is the performance that we are measuring right now doesn&#x27;t represent the real workload. It&#x27;s not just this slide when you talk about agentic workflows even talking about performance become little tricky. You can&#x27;t talk about mean and medians. Now you have to talk about in terms of</p>
<p>Now you have to talk about in terms of distributions and what I&#x27;m showing here is kind of two distributions and you can think of the first one is time to first token. So this is how fast you get the token back from the LLM. The right one is token speed. So how fast your token your LLM is actually producing the tokens. And the way you produce these histograms, you run your workload against a hardware. You capture all the requests that are going to your large language models.</p>
<p>language models. You c you you collect all the time to first tokens that you get for all these requests and you create this histogram. So left is good that means you are getting your response back very fast in this TTFT. write is bad. That means your your token is taking longer and longer for you LLM is taking longer and longer for you to get back the response. Whereas on the on the other distribution, left is bad because that&#x27;s slow generation of tokens, right? You</p>
<p>slow generation of tokens, right? You you&#x27;re generating more tokens and the way and and your user experience kind of dies on those tails. So even if a lot of your requests sit in this, if the users start experiencing this tail, their experience is going to be heard. So you want to make sure you clip these P95 to a reasonable range that that kind of matches with your customer&#x27;s experience. So all this slide is trying to say in</p>
<p>So all this slide is trying to say in the agentic workload you can&#x27;t just talk about mean median you have to look at distributions you have to talk about metrics in terms of distributions in terms of P95 P25 and and P95 if you if you don&#x27;t remember kind of your stats world P95 means 95 of the requests 95% of the request have time to first token better than this value P25 means 75 5% of the requests have faster speed than that p25 value.</p>
<p>The so this is this is kind of a a deeper dive a little bit on the distribution. Uh so ignore if you don&#x27;t kind of follow this slide but just wanted to mention this. Um again I&#x27;m I&#x27;m plotting the same distribution that was in the previous slide for speed. how fast your tokens are producing the speed. But now I have two distributions. On the green one I have speculative decoding on. So this is the optimization that I talked about where you can</p>
<p>that I talked about where you can generate three tokens in one go versus just generating one token at a time. And what you see is the distribution is towards the right. So the speeds in general are much higher for this uh uh distribution with with MTP or or speculative decoding on whereas distribution for for blue which does not have speculative decoding on is actually much slower than than the other one. But if you&#x27;re not looking at the right metric, for example, if I look at the P5</p>
<p>metric, for example, if I look at the P5 value, which is uh 95% of my requests are producing outputs faster than this value for this distribution and 95% of my requests are producing faster uh tokens than this value for this distribution. Your situation will be reversed. The green looks worse here whereas the blue looks better and which is not the complete picture. You are actually penalizing a very good optimization where most of your requests are actually</p>
<p>where most of your requests are actually sitting on the right side just because you chose the wrong in this sense I&#x27;m saying wrong. Uh but for some people maybe P5 is the criteria that they want to satisfy. For our case, what turned out was instead of P5, we should be looking at P25 because what we are doing is we are unnecessarily penalizing kind of an artifact of speculative decoding. Again, the goal if you don&#x27;t understand the detail that&#x27;s fine. The goal of this</p>
<p>the detail that&#x27;s fine. The goal of this slide is when you&#x27;re thinking about percentiles, when you&#x27;re thinking about SLO, you need to take a deep deeper dive on on what uh what different optimizations are doing to your distributions and how do you want to match them against customer expectations. So, it does require kind of a little bit more uh in-depth kind of digging into your data rather than kind of simple chatbot style workloads. The the other thing that you need to think about</p>
<p>think about uh so I have two colorful plots here different rows are basically user user one user two user three user four and what this slide is trying to convey is if you ignore the tools tool part of it you&#x27;re not really measuring the right workload again so on on the left side I&#x27;m assuming there is no tool call so there is none of that build purple block that I showed where where the um kind of things are running on the CPU. In that</p>
<p>things are running on the CPU. In that case, what you end up saying is user one, user two, user three, user four are fully packing my LMS or fully packing my GPUs. So this whole greens is basically when your large language model is being called. But in reality, that&#x27;s not the case. When you&#x27;re running a workflows, this is not how your GPUs look like. you&#x27;re you&#x27;re not continuously kind of keeping your GPUs busy. What is happening is you run your large language models and then actually you are running tool calls</p>
<p>actually you are running tool calls which are which are running on your CPU. So actually your GPU is not doing any work during that time. And depending on how big that gray region is, you can actually support more users because you are effectively not packing your GPUs as much as you can. So just ignoring the concept that there is no tool actually ends up uh predicting the wrong concurrency or wrong workload that that you that you can support on</p>
<p>that that you that you can support on your hardware. And there are some metrics here like if you assume one LM call takes 1 second. If you have a tool call that takes 1 second too and half of your area is gray there you can actually support twice the number of users than you thought because a bunch of that workload is being done on CPUs too. So again the the crux of this slide is make sure you&#x27;re representing your workload correctly especially in agentic workloads those gray regions where this stuff is being run on CPU is very important and that</p>
<p>run on CPU is very important and that kind of helps you getting the right number of kind of users you can support for a given workload. So that that&#x27;s the tool side of things. Now this this is the last piece on on kind of what is missing in the current workloads. Um and this is again a subtle piece when you&#x27;re talking about agentic workloads. Again the metrics takes some time to stabilize. So if you&#x27;re looking at the same metrics that I talked about the time to first token and speed</p>
<p>time to first token and speed you can see like in the beginning when the cache is not warmed up things are still in the transient state your metric is still evolving. So if you would have measured your performance at this point and assumed that your SLO was 10 seconds for TTFT, you would have said that this deployment is is fail fails that SLO, which is not the case. It&#x27;s just that this is still in the transient state. It&#x27;s not stabilized yet. And that is just because again with the agentic</p>
<p>is just because again with the agentic workflows, the caches are coming into play. There are much more higher level of optimizations that are coming into play. So they they need time to stabilize. So make sure when you&#x27;re looking at this metrics uh you are sure that the metrics that you are kind of focused on is has stabilized and some curves like that where you plot the metrics over time and see if you see flat flatness towards the end is is a way to do that. Um and just to reiterate the same point the same story here holds with speed. So if if</p>
<p>story here holds with speed. So if if these were your SLO lines, you would have said that this is a bad deployment and you would have said no, we need either more GPUs. Whereas if you would have just let the system stabilize, you would have said this is a perfectly well deployment which matches both of the SLOs&#x27;s. So now I just want to give you an example of a benchmark that is trying to close this gap. U this is a benchmark from artificial analysis. It&#x27;s a third party benchmarking uh company and they came up with this uh benchmark couple of</p>
<p>came up with this uh benchmark couple of weeks ago. Um the results are still not out. So in in a few weeks of time you can see different hardware players and how they do on this benchmark but it does enable most of the things that we talked about KB cache aing multi-turn the speculative decoding the SLO based setup that we just talked about. It&#x27;s it&#x27;s for two models Deepseek V4 and GPTUS 120B for now and this this list will glow grow uh and the way they are going to and the and the question that</p>
<p>going to and the and the question that they are trying to answer is they have given three SLOs&#x27;s for example here where they have SLO on how fast your LLM should generate the tokens what is the time to first token and now you can imagine a another column here which says to meet this SLO for a given hardware how many users can I sustain so that&#x27;s the question that they&#x27;re trying to answer for each of these SLOs&#x27;s what&#x27;s the maximum number of users a given hardware can sustain and you</p>
<p>a given hardware can sustain and you should see numbers for different hardwares and things like that and they have curated this whole data set multi-turn trajectory uh real world data set so this is trying to close that gap in in addition to that it will also show uh kind of per watt metrics and and kind of some some per dollar metrics also. So keep an eye out. Artificial analysis is uh this benchmark is is going to start kind of closing some of the gaps that I talked about and it&#x27;s going to get more</p>
<p>talked about and it&#x27;s going to get more and more towards the real agentic workloads that that most of the productions optimizations are running. Um just bringing everything together. We talked about multi-turn trajectories very important. We talked about different kinds of optimization, caching and routing, spec decoding. We talked about how to think about performance in terms of SLOs&#x27;s rather than mean and medians. We talked about how tools become very important in this</p>
<p>how tools become very important in this case and we talked about the steady state to make sure that when you&#x27;re measuring some metrics that is you&#x27;re not measuring something that is still in the transient state and has has stabilized. Again, as I said, this is still the beginning. This is like the bread and butter of agentic workloads. What&#x27;s still missing is is is a lot and and it&#x27;s still this field is going to still evolve and we are going to get better and better. Multi-agent workflows is still I&#x27;m talking about single trajectory one agent doing interacting with tools. How about when multiple agents are interacting? What do you</p>
<p>agents are interacting? What do you think? How do you think about kind of benchmarking in that case quality under load? I kind of skipped the whole accuracy piece. How good your agent is, how fast it can solve something. I primarily focused on performance but you do need to couple these things together. How fast your model can run and how how good is the model actually in solving a particular task. Long running sessions agents are becoming longer and longer. You can let the agents run for two days, 3 days. How do you think about performance there? Because now your KB cache or cache is continuously growing.</p>
<p>cache or cache is continuously growing. You need to manage that memory somehow. Heterogeneous workload. This was all coding. How about different kinds of agentic workloads? CPU coupling. We didn&#x27;t even talk about how the CPU comes into play. We just kind of talked about delays and things like that. But in this case now GPU and CPU have to play together to make these uh workloads run efficiently and then different kinds of metrics you can talk about task completion time rather than what what I talked about. So a lot of work on on what a good metrics look like. Uh so so</p>
<p>what a good metrics look like. Uh so so in in short today&#x27;s benchmarks have caught up to chatbot area uh kind of chatbot world but the benchmarks for agentic world uh are still being built and and we are just getting started. Uh there&#x27;s still a long way to go. Uh but benchmarks like artificial analysis agent per benchmark is trying to close that gap. U yeah with with that I&#x27;m going to end my talk and happy to take any questions YOU HAVE.</p>
<p>UM, DO WE HAVE ANY QUESTIONS? OH, one very eager question down here. Can we get a mic down here, please, Prince?</p>
<p>There you go.</p>
<p>Thank you for the nice presentation. And I have a question about speculative decoding. Uh does it work with non-zero model temperatures or is this all just about benchmarking assumes that all</p>
<p>about benchmarking assumes that all users on server use uh zero model temperatures?</p>
<p>No, it does work with non-zero non-zero temperatures.</p>
<p>Can you give us a bit details how cuz I don&#x27;t imagine</p>
<p>so so</p>
<p>our model can verify something that was randomly picked from choices.</p>
<p>So so so so the distribution is still matches. So, so when we talked about kind of matching the distributions of these two, so distrib you&#x27;re still sampling from a distribution, right? Even for a speculative decod, you&#x27;re not just randomly going and picking up anything. So, if your distribution is still matches, you are still have higher</p>
<p>still matches, you are still have higher probability of predicting what the target is going to produce, right? So, so it still still holds. Yeah.</p>
<p>Thank you.</p>
<p>Okay. I think we had another question over here. Thank you for your talk. Um, your time to fast token and speed histograms, are you running those on the same uh</p>
<p>you running those on the same uh workflow, the same request or are you doing that across your kind of whole suite of kind of agent requests? This is for uh do you mean is it just agentic or is the mixture</p>
<p>kind of like a sing are you running the same prompt through that uh</p>
<p>no no this whole trajectory so that that that example that I had in the beginning was just an example of a trajectory</p>
<p>u and you can imagine like hundreds and thousands of these things that we have collected and running against the against the hardware to get these</p>
<p>against the hardware to get these numbers. So you would with that you would expect a harder problem to be longer to the first token anyway though wouldn&#x27;t you?</p>
<p>So so this is where caching comes into play right? So so if if most of your prefill is cached what you&#x27;re doing the work to get the test time to first token is just this new extra work. If the caching was off, yes, you at the last kind of at this point you will be doing something like that which is doing competition for this whole input input</p>
<p>competition for this whole input input field which yes obviously your TTFT will be much worse but because of caching you&#x27;re your your TTF is kind of just constrained by that new piece and I think you&#x27;re also trying to ask is because of the trajectory the time to first so the time to first token here still means for a request for one of these kind of one of these green blocks time to first token is not when your answer starts coming I think so that that&#x27;s another</p>
<p>no that makes sense that yeah you got it</p>
<p>no that makes sense that yeah you got it bang on um yeah I was just thinking if if for example one of your tool calls uh came out with like a really long response and and it really struggled to chime through that then you&#x27;d expect a really high peak to get to that time to first token because it would be working through a harder problem.</p>
<p>Exactly. And that that&#x27;s why prefix caching is like the bread and butter for optimizing these aentic workloads. And it&#x27;s a shame that agent most of the benchmarks don&#x27;t capture that and don&#x27;t let you capture that. So yeah,</p>
<p>really interesting. Thank you.</p>
<p>Okay. Um I&#x27;m afraid we&#x27;re out of time there, but would you be happy to answer a few questions?</p>
<p>a few questions?</p>
<p>Yes, I&#x27;m here. So</p>
<p>So yeah, please do</p>
<p>come and find a for more questions. Um but in the meanwhile, thank you very much, Amit.</p>

</details>
